{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "devtools": {
      "type": "local",
      "command": ["npx", "-y", "@modelcontextprotocol/server-everything"],
      "enabled": true
    }
  },
  "agent": {
    "default": {
      "instructions": "This project uses pnpm. Always use pnpm instead of npm unless explicitly told otherwise."
    }
  }
}